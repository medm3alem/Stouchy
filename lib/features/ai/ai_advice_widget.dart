import 'package:flutter/material.dart';

class AiAdviceWidget extends StatelessWidget {
  const AiAdviceWidget({super.key});
  @override
  Widget build(BuildContext context) {
    return const Card(child: Padding(padding: EdgeInsets.all(8.0), child: Text('Conseil IA')));
  }
}
